# Pandatrack

A time-tracker for iOS.

## Development

Install CocoaPods dependencies before opening the project:

```sh
pod install --repo-update
open Chronos.xcworkspace
```

Run Xcode static analysis with the repository-local lint command:

```sh
./scripts/lint.sh
```
